{
  "name": "agent-bazaar",
  "private": true,
  "version": "0.1.0",
  "description": "Agent Bazaar — an x402 agent-services marketplace on Hedera, Arc and The Graph (ETHOnline 2026)",
  "license": "MIT",
  "packageManager": "pnpm@10.32.1",
  "engines": { "node": ">=22" },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "tsx": "^4.20.5"
  },
  "pnpm": {
    "overrides": {
      "@hiero-ledger/sdk": "2.87.0"
    }
  }
}
