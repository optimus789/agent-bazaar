# STATUS — verified evidence per work package

Record only what was actually run. Every entry needs a date, network, and a link (tx hash, subgraph query URL, mirror node URL).

## WP00 environment
- 2026-09-06: `curl https://api.testnet.blocky402.com/supported` → lists `hedera:testnet` with feePayer `0.0.7162784`. ✅
- npm packages verified present: `@x402/hedera 2.25.0`, `@x402/fetch 2.25.0`, `@x402/express 2.25.0`, `@graphprotocol/client-x402 1.0.0`, `@circle-fin/x402-batching 3.4.0`, `@hiero-ledger/sdk 2.87.0`, `@hashgraph/hedera-agent-kit 4.1.0`.
- 2026-09-06: `.env` populated — Anthropic key (model set to `claude-haiku-4-5-20251001` for cost), Graph Studio key, two fresh Base Sepolia EVM keys (buyer/provider, unfunded), two fresh Arc testnet EVM keys (seller/buyer, unfunded), two Hedera testnet ECDSA accounts from portal.hedera.com (buyer `0.0.10392709`, provider `0.0.10392781`, each auto-funded 1000 test ℏ), `SELLER_ADMIN_TOKEN` generated.
- 2026-09-06: Hedera USDC (`0.0.429274`) associated with both accounts:
  - buyer: https://hashscan.io/testnet/transaction/0.0.10392709@1788710720.074559531 (SUCCESS)
  - provider: https://hashscan.io/testnet/transaction/0.0.10392781@1788710729.297821534 (SUCCESS)
- 2026-09-06: Circle testnet USDC faucet funded the provider account (20 USDC) instead of the buyer; rebalanced with a real Hedera token transfer, provider → buyer, 15 USDC: https://hashscan.io/testnet/transaction/0.0.10392781@1788712186.323130080 (SUCCESS). Final balances confirmed via mirror node: buyer 15.000000 USDC, provider 5.000000 USDC. ✅ first real on-chain activity in the project (previously all mock/fixture).
- 2026-09-06: Base Sepolia funded — buyer `0xf580357aa3A2AcC467982F0000e6a5Baa38b4Da1` (0.02 ETH + 20 USDC), provider `0x66603CFFcDbF3b39785afD82F6F396a13C5C605a` (0.02 ETH, no USDC needed — only registers its own ERC-8004 identity). Verified via `eth_getBalance`/`eth_call` on `sepolia.base.org`. ✅
- 2026-09-06: Arc testnet funded — seller `0xc056c13f5cAb87365a9e0DB957A61805218f88A0` and buyer `0x14937a696ee229288aAC7E332B3Eb910148691aD`, 20 USDC each (native gas token on Arc, 18dp). Verified via `eth_getBalance` on `rpc.testnet.arc.io` (chain id `0x4cef52` = 5042002, matches spec). ✅
- **WP00 funding checklist complete.** All accounts across Hedera testnet, Base Sepolia, and Arc testnet are funded and associated. Ready to move WP02–WP07 from mock/fixture mode to real `--live` runs.

## WP01 scaffold
- 2026-09-06: pnpm workspace + `@bazaar/shared` (chains, x402 prices, Graph URLs, env, JSONL log). `pnpm -r typecheck && pnpm -r test` green (14 tests). ✅
- `associate-usdc` script written, not yet run (needs Hedera accounts from WP00).

## WP02 graph
- 2026-09-06: `@bazaar/graph` written: StudioClient, X402GraphClient (`@graphprotocol/client-x402`), MockGraphClient + fixtures; Agent0 `listAgents`/`getAgent` (one document, two chains); Messari `topPools`/`poolMetrics`. 17 tests green offline. ✅ (offline)
- Agent0 subgraph ids from thegraph.com docs: Base Sepolia `4yYAvQLFjBhBtdRCY7eUWo181VNoTSLLFd5M7FXQAi6u`, Ethereum Sepolia `6wQRC7geo9XYAhckfmfo8kbMRLeWU8KQd3XsJqFKmZLT`.
- ⚠️ Messari DEX subgraph id `FUbEPQw1oMghy39fwWBFY5fE6MXPXZQtjncQy2cXdrNS` ("Uniswap V3 Base") is UNVERIFIED as Messari-schema. Override with `MESSARI_DEX_SUBGRAPH_ID`. Verify with a Studio key: query `{ liquidityPools(first:1){ id } }` must succeed.
- Live demos (`demo:agent0`, `demo:x402`, `record-fixtures`) not yet run: need GRAPH_API_KEY / Base Sepolia USDC.

## WP03 hedera provider
- 2026-09-06: `apps/provider-hedera` written: 4 paid routes, deterministic `riskScore`, x402 middleware via `@x402/express` + `@x402/hedera`, facilitator default `https://api.testnet.blocky402.com`, HCS receipt sink (mock JSONL / live topic). 11 tests green.
- MOCK smoke over HTTP: unpaid POST → `402` with `accepts[0] = { scheme: exact, network: hedera:testnet, asset: 0.0.429274, amount: 2000, payTo, extra.feePayer: 0.0.7162784 }`. ✅
- 2026-09-06: **Real paid request completed end-to-end on Hedera testnet.** Booted `provider-hedera` live (`MOCK=0`), which created a fresh HCS receipt topic `0.0.10395241` and confirmed the Blocky402 facilitator's `/supported` sync at boot. Buyer account `0.0.10392709` called `POST /v1/usdc/brief/pool` via the exact `apps/buyer-agent/src/rails/hedera.ts` client (`wrapFetchWithPayment` + `ExactHederaScheme` + `createClientHederaSigner`) — unpaid request confirmed 402 first, then the real paid call settled:
  - Settlement tx: `0.0.7162784-1788716210-213797949` (Blocky402's own fee payer `0.0.7162784` submitted it) — mirror node confirms a real `CRYPTOTRANSFER` of HTS token `0.0.429274` (USDC), `-2000`/`+2000` (0.002 USDC, 6dp) from buyer `0.0.10392709` to provider `0.0.10392781`, status `SUCCESS`.
  - HCS receipt: topic `0.0.10395241`, sequence 1 — mirror node message decodes to `{route, payer: 0.0.10392709, amount: "$0.002", asset: USDC, txId, briefHash, usage: {graphQueries:1, llmTokens:86, ms:4239}}`, matching the settlement exactly. Mirror URL: https://testnet.mirrornode.hedera.com/api/v1/topics/0.0.10395241/messages
  - Response included `X-Bazaar-Usage` metering header and a real Messari-derived risk brief (riskScore 50, reasons, metrics) for a live Base Uniswap V3 pool — confirms the Graph-sourced reasoning path, not a canned response.
  - `HCS_RECEIPT_TOPIC_ID=0.0.10395241` persisted to `.env` for reuse on future boots.

## WP04 identity
- 2026-09-06: `@bazaar/identity` written: HCS-14 UAID (`computeAid`/`wrapExistingDid`), registration file builder, `register.ts`/`feedback.ts` against the **real** ERC-8004 ABIs pulled from github.com/erc-8004/erc-8004-contracts (not guessed — confirmed `register(string agentURI) returns (uint256 agentId)`, `Registered(agentId indexed, agentURI, owner indexed)` event, and `giveFeedback(agentId, value int128, valueDecimals uint8, tag1, tag2, endpoint, feedbackURI, feedbackHash)` — note the `endpoint` param the brief's paraphrase omitted). 26 tests green.
- ⚠️ HCS-14 has no published test-vector table with concrete hash outputs. Verified by installing the reference `@hashgraphonline/standards-sdk@0.1.186` in an isolated throwaway project on 2026-09-06 and diffing byte-for-byte: canonical JSON key order is the FIXED sequence `skills,name,nativeId,protocol,registry,version` (not alphabetical, not input order); hash = sha384(canonicalJson) → plain Base58 (Bitcoin alphabet, no multibase prefix); `computeAid`'s default UAID auto-populates `uid=0`, `registry` and `nativeId` from the canonical data even with no params passed; `wrapExistingDid` adds NO defaults and also strips the DID method name, not just `did:` (e.g. `did:web:example.com:agents:1` → `example.com:agents:1`). Four vectors pinned as regression tests in `packages/identity/test/uaid.test.ts`. Did NOT vendor the reference SDK as a dependency (923 transitive packages, 53 vulnerabilities including 1 critical) — reimplemented the ~40-line algorithm standalone instead.
- Two registration files generated (`registrations/provider-hedera.json`, `registrations/provider-arc.json`) with PLACEHOLDER native ids — see `registrations/README.md`. Not yet registered on-chain (needs WP00 Hedera/EVM keys).
- 2026-09-06: **Real on-chain registrations, all three complete:**
  - Bazaar Market Intel (Hedera) on **Base Sepolia**: agentId `9179`, owner `0x66603CFFcDbF3b39785afD82F6F396a13C5C605a`, tx https://sepolia.basescan.org/tx/0x7c489b270f2eb9056015319785c1cc9a678274a5391d00729006866f53075f3c
  - Bazaar Market Intel (Hedera) on **Hedera testnet**: agentId `109`, tx https://hashscan.io/testnet/transaction/0x1d7bb528de3dd7d0fc12c3ddc2a72f6dbb764000c5938b0c527b6f0ae4a5d521
  - Bazaar Task Runner (Arc) on **Base Sepolia** (Arc itself has no ERC-8004 deployment): agentId `9180`, tx https://sepolia.basescan.org/tx/0x5ddf8d1cbe60641a881d56a101db731f4c2a102efdca181bc7442176a5f7e2a8
  - tokenURI for both providers now points at `https://raw.githubusercontent.com/optimus789/agent-bazaar/main/registrations/{provider-hedera,provider-arc}.json` (repo made public in WP00 follow-up specifically to satisfy this requirement); registration files rebuilt with real native ids, real priced routes, and the on-chain agentIds above.
  - ⚠️ Real gotcha found: registering "on Hedera testnet" still signs with `EVM_PROVIDER_PRIVATE_KEY` (an ordinary EVM key), because the ERC-8004 registry on Hedera testnet is only reachable through Hashio's EVM JSON-RPC layer — a native Hedera portal account's HBAR balance is invisible to `eth_getBalance` on Hashio until that EVM address has an activated alias. Fixed by sending a small HBAR transfer from the funded native Hedera provider account (`0.0.10392781`) directly to the EVM address, which auto-creates the alias; confirmed via `eth_getBalance` on Hashio before/after.
  - Not yet done: confirm Agent0 subgraph indexing on Base Sepolia (needs the mempool/indexer to catch up, and raw.githubusercontent.com's cache to serve the updated file — content confirmed correct via GitHub's contents API, propagation to the raw CDN is a few minutes), live feedback tx from the buyer key.

## WP05 arc provider
- 2026-09-06: `packages/shared/src/arcBuyer.ts` (spending-cap wrapper around Circle's real `GatewayClient`, using its own `onBeforePaymentCreation` hook — confirmed against `@circle-fin/x402-batching@3.4.0` dist typings, not guessed) and `apps/provider-arc` (summarise + classify-risk routes behind `createGatewayMiddleware`, earnings ledger, admin-token-gated withdraw via `GatewayClient.withdraw()`). 42 new tests green (18 + 21 + updates), typecheck clean.
- Confirmed from the SDK's own `.d.ts` (not the brief's paraphrase): `chain: 'arcTestnet'` is the exact literal (GATEWAY_DOMAINS enum); `GatewayClient.pay()` returns `amount` as a `bigint` in atomic USDC (6dp), not a string; `getBalances()` and `withdraw()` also return bigints that must be converted to the SDK's own `formatted*` strings before `res.json()` (bigint fields throw on JSON.stringify) — fixed in both the `/v1/seller/earnings` and `/v1/seller/withdraw` routes.
- MOCK smoke over real HTTP: unpaid POST → 402 with a Gateway-shaped `accepts[0]` (network `eip155:5042002`, payTo = seller); paid summarise and classify-risk both settle and post to the ledger ($0.001 + $0.0005 = $0.0015 total, matches exactly); classify-risk correctly cross-references a Hedera-shaped Brief object (label `high`, confidence 0.7) — this is the cross-provider workflow the buyer agent will run in WP06. ✅ (mock)
- Not yet done: a real Gateway deposit + nanopayment + withdrawal on Arc testnet (needs WP00's `ARC_SELLER_ADDRESS`/`ARC_SELLER_PRIVATE_KEY`/`ARC_BUYER_PRIVATE_KEY` and testnet USDC from faucet.circle.com), Circle Agent Marketplace listing.

## WP06 buyer agent
- 2026-09-06: `apps/buyer-agent` written: `ledger.ts` (hard USD cap via `reserve()`/`record()`), `ranking.ts` (reputation/affordability/validation scoring with unaffordable providers scoring `-Infinity`), three rails (`rails/hedera.ts` using the exact `@x402/fetch` + `@x402/hedera` pattern verified from the WP03 reference PoC, `rails/arc.ts` wrapping WP05's `makeArcBuyer`, `rails/graph.ts` wrapping WP02's `X402GraphClient`), seven `ai` SDK tools, `agent.ts` (the `generateText` + `stepCountIs` loop), and `index.ts` (CLI with `--mock`/`--live`). 23 new tests (116 total across the repo), typecheck clean.
- Verified against real package internals rather than the brief's sketch: `ai@7.0.93`'s `tool()` builder takes `inputSchema` (not `parameters`, which was renamed in AI SDK v5+) and is re-exported from `@ai-sdk/provider-utils`; confirmed by round-tripping a hand-built `LanguageModelV2`-shaped mock model through a real `generateText` call before writing any agent code. `createOpenAICompatible({ baseURL, name, apiKey })` + `.chatModel(id)` confirmed for the local-model (Qwen/LM Studio) fallback path.
- ⚠️ Found and fixed a real dependency-resolution bug: `@x402/hedera` pins `@hiero-ledger/sdk@^2.85.0` internally while our own packages pin `^2.87.0`, so pnpm kept two structurally different (but same-named) `PrivateKey` classes and `createClientHederaSigner` failed to typecheck across the boundary. Fixed with a `pnpm.overrides` entry pinning the whole workspace to one version — a real cross-package integration issue that unit tests within a single package would never have caught.
- The agent-loop test suite (`test/agent.test.ts`) uses a scripted `LanguageModelV2` stub — no LLM call, no network — and proves the important safety property precisely: when the model tries to overspend, `BudgetLedger.reserve()` throws before any rail is touched; `generateText`'s own tool-error recovery feeds that failure back to the model as a tool result (not a thrown exception out of `runAgent`); the test asserts `ledger.spentUsd === 0` afterward, i.e. no money moved. Also verified: chaining a Hedera + Arc purchase in one run, one JSONL decision-log line per tool call, and `maxSteps` actually bounds the loop.
- Not yet done: no real LLM call has been made (no `ANTHROPIC_API_KEY` and no local LM Studio server available in this environment) and no real `--live` run against the chain has happened — both are blocked on WP00. `docs/demo/transcript-*.md` will only be written by a real `--live` run.

## WP07 dashboard
- 2026-09-06: `apps/dashboard` written (Next.js 16.3.4 App Router, Tailwind v4): `/` marketplace (Agent0 discovery + live provider health), `/provider/[chain]/[agentId]` (registration file, live catalog, recent settlements), `/agent` (SSE-tailed live decision log with a spend counter), `/payments` (unified ledger across all three rails from every JSONL source), `/seller` (Arc earnings, Gateway balance, a real withdraw form proxied server-side so the admin token never reaches the browser).
- ⚠️ Found and fixed a real Turbopack bug: Next.js 16.3.4's Turbopack (the new default bundler) cannot resolve the monorepo's `.js`-suffixed relative imports that resolve to `.ts` files on disk (the standard NodeNext/tsc/tsx/vitest convention used everywhere else in this repo) — it reports "the module has no exports at all" even with `transpilePackages` and an `extensionAlias` set. Confirmed Turbopack-specific by building the identical config with `--webpack`, which works cleanly. Fixed by pinning `apps/dashboard`'s `dev`/`build` scripts to `--webpack` rather than rewriting the monorepo's import style.
- ⚠️ Found and fixed a real cross-package bug while testing: `discover_providers` (WP06) hydrates each Agent0 listing's routes from the provider's own `/catalog` before ranking, but the dashboard's marketplace page did not do the same, so every provider showed "no priced routes" despite being reachable. Moved the hydration logic to `packages/graph/src/hydrate.ts` (`hydrateCatalogs`) so both the buyer agent and the dashboard share one implementation; verified live in the browser before and after.
- ⚠️ Found and fixed test-data pollution: `apps/provider-arc`'s `Ledger`, `apps/provider-hedera`'s `JsonlReceiptSink`, and `apps/provider-hedera`'s `server.ts` all defaulted to the real `jsonlLogger(...)` with no injection point, so every test run silently appended fake payments to the shared `data/*.jsonl` files the dashboard reads. Added constructor/`AppDeps` injection points to all three and updated their test suites to use isolated `mkdtemp` directories (matching the pattern already used in `packages/identity/test/register.test.ts`). Verified `data/` stays empty after a full `pnpm -r test` run.
- Verified end-to-end in a real browser (Playwright + the browser tool) against the two providers running in MOCK mode with seeded JSONL data: marketplace shows live prices and health; provider detail shows the real catalog and settlement history; **the agent page's SSE stream picked up a newly appended `data/buyer.jsonl` line within ~1s with no page reload** (the acceptance criterion's "updates in real time"); payments page aggregates all three rails with a total that matches the seeded amounts exactly; seller page's withdraw button round-trips through the dashboard's own `/api/seller/withdraw` proxy to the live provider-arc process and correctly surfaces its real "no live GatewayClient in this mode" error in MOCK mode. Two real paid HTTP requests were made against the live provider-arc process (not just seeded JSONL) to populate its in-memory ledger for the seller-page screenshot.
- Screenshots saved to `docs/demo/screens/`: marketplace.png, provider-detail.png, agent.png, payments.png, seller.png.
- Not yet done: Lighthouse accessibility audit (needs a browser devtools run, not attempted here); a real `--live` buyer-agent run driving `/agent` (blocked on WP00 keys, same as prior WPs).

## WP08 arc mainnet
- (fill in after 16 Sept)
